linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - unused

issues:
  exclude-dirs:
    - examples
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - staticcheck
        - gosimple
        - unused
        - ineffassign
