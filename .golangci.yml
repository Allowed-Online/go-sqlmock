version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
  exclusions:
    rules:
      - path: _test\.go
        linters: [errcheck, staticcheck, unused, ineffassign]
      - path: examples/
        linters: [errcheck, staticcheck]
